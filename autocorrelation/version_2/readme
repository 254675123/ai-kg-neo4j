第二个版本的自动关联
知识点，在word文档里面，docx格式，一门课程一个docx，课程有一个excel名单
试题，在excel文档里面，所有的课程都在一个文件中

对于一门课程，执行如下操作：

1. 先用课程的课件，生成txt文件，一来用来抽取知识点，二来用来作为训练模型语料的一部分，
   抽取的知识点，也作为训练模型语料的一部分

2. 再把excel里面的课程试题，分拆出来，单独存储文件，作为训练模型语料的一部分

3. 训练模型，对试题和知识点的相似度，进行匹配。
